package org.example.DesignPatterns.Strategy.PathCalculatorStrategies;

import org.example.DesignPatterns.Strategy.PathCalculator;
import org.example.DesignPatterns.Strategy.Route;

public class CarPathCalculator implements PathCalculator {
    @Override
    public Route findPath(String src, String dest) {
        // Complete this function
        return new Route();
    }
}
